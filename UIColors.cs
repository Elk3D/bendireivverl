using UnityEngine;

public static class UIColors
{
	public static Color ACTIVE_LABEL => new Color32(byte.MaxValue, 193, 97, byte.MaxValue);

	public static Color ACTIVE_LABEL_REAL => new Color32(102, 156, 162, byte.MaxValue);

	public static Color INACTIVE_LABEL => new Color32(130, 77, 12, byte.MaxValue);

	public static Color INACTIVE_LABEL_REAL => new Color32(59, 100, 104, byte.MaxValue);
}
